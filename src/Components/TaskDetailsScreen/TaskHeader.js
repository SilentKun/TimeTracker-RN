import React from 'react';
import {StyleSheet, Text, TouchableOpacity, View} from 'react-native';
import ProjectsCell from '../ProjectsScreen/ProjectsCell';

const TaskHeader = () => {
    <View>

    </View>
};

const styles = StyleSheet.create({
    container: {
        marginBottom: 10,
        backgroundColor: '#FFF',
        elevation: 5,
        // width: '100%',
        height: 100,
        borderRadius: 3,
        flexDirection: 'column',
    },

});

export default TaskHeader;
